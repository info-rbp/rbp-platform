# Appwrite Inventory

## Repository-Owned Resources

- one database: `rbp_platform`
- one primary document bucket: `rbp_documents`
- collection JSON files under `appwrite/collections/`
- function entrypoints under `appwrite/functions/`
- QA seed files under `appwrite/seeds/qa/`

## Inventory Status

Live inventory still requires reachable Appwrite credentials. Until those are present, the repository definition is the authoritative desired state.
